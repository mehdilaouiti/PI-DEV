{{ form_start(form, {attr: {class: 'filter'}}) }}
{{ form_row(form.q) }}
<h6 class="pro-d-title">Categories</h6>
{{ form_row(form.categories) }}
<h6 class="pro-d-title">Fabricant</h6>
{{ form_row(form.fabricants) }}
<h6 class="pro-d-title">Prix</h6>
<div class="row">
    <div class="col-md-6">
        {{ form_row(form.min) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.max) }}
    </div>

</div>
<button type="submit" class="btn btn-primary w-100">Filtrer</button>
{{ form_end(form) }}
